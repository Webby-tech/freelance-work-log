DATABASE_URL=
APP_PASSWORD=
# Receipt uploads: read-write token of a PRIVATE Vercel Blob store (added automatically
# when the store is connected to the project — see the receipt setup steps)
BLOB_READ_WRITE_TOKEN=
